# .env.example
# Copia este archivo a .env y configura tus credenciales

# Base de Datos
DB_HOST=localhost
DB_NAME=sistema_pos
DB_USER=root
DB_PASS=

# Configuración del Sistema
APP_NAME="Mi Sistema POS"
APP_ENV=development  # Cambiar a production en servidor
APP_DEBUG=true       # Cambiar a false en producción
APP_URL=http://localhost/pos

# Seguridad
SESSION_LIFETIME=7200
SESSION_NAME=pos_session
BCRYPT_ROUNDS=12

# Opciones de Email (si aplica)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=